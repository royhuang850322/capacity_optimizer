# Legacy Excel Control Panel Archive

This folder keeps the retired Excel control workbook UI for historical reference:

```text
Capacity_Optimizer_Control.xlsx
```

Current users should run the tool through:

```text
CapacityOptimizer.exe
CapacityOptimizerLauncher.pyw
```

The workbook is no longer copied into customer delivery packages and is no longer the primary UI.
